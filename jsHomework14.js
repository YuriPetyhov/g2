// Напишите код, выполнив задание из каждого пункта отдельной строкой:

const user = {};
user.name = 'John';
user.surname = 'Smith';
user.name = 'Pete';
delete user.name;

// Можно изменять значения свойств объекта, даже если оъект является константой